```
请在当前项目的rustvulnweb 目录下，编写一套基于Actix-web框架的Rust Web测试代码，用于安全代码分析工具、漏洞检测工具的深度能力验证。整体遵循分目录、分文件原则，结构规范、注释清晰、代码简洁无冗余。

一、整体目录与文件拆分规则
1. 公共父类、通用子类统一放置在 common 目录；
2. trait定义单独存放至 trait_def 目录，trait与实现结构体拆分为独立文件；
3. 按照漏洞类型划分独立子目录：cmd_exec（命令执行）、code_exec（代码执行）、ssrf（SSRF漏洞）；
4. 每个漏洞目录下区分两类测试文件：纯结构体组合调用文件、trait实现类调用文件；
5. 根目录保留总入口文件 main.rs、通用跨文件函数调用文件 func_call.rs。

二、代码功能与场景要求
1. 漏洞类型要求
   集成命令执行、代码执行、SSRF三类高危漏洞，同时包含两种数据源场景：
   - 外部可控数据源：通过 Actix-web HttpRequest 接收前端传入参数，构造完整漏洞调用链；
   - 硬编码无外部数据源：内置固定参数的漏洞代码；
   所有漏洞点嵌入到父结构体方法中，通过「结构体实现、trait实现、跨文件调用」形成多层深层调用链。

2. 面向对象调用场景（全覆盖）
   - 基础调用：同一文件内函数/结构体调用、跨文件函数调用、跨文件结构体实例与方法调用；
   - 结构体组合：编写独立父结构体 VulnParent、独立子结构体 VulnChild，实现子结构体包含父结构体、调用父结构体漏洞方法的链路；
   - trait能力：独立定义trait文件，每个漏洞目录编写对应的实现结构体，实现「trait + 结构体组合」组合调用；
   - 多层调用链：构造 外部请求参数 → 调用入口 → 子结构体/trait实现结构体 → 父结构体 → 漏洞点 的完整长调用链路。

3. 代码规范要求
   1. 每个文件功能单一、职责清晰，漏洞位置、调用关系添加明确注释标注；
   2. 所有文件路径引用合理，模块导入无路径错误；
   3. 总入口页面整理所有测试链接，方便直接访问各个测试用例；
   4. 保留纯结构体组合调用测试用例、trait实现类测试用例两类场景，分开独立测试。

三、输出要求
1. 先输出完整的目录树结构；
2. 再按目录顺序，依次输出每一个文件的完整可运行Rust代码；
3. 代码保证可直接部署运行，无需额外配置。
```

---

## 补充目录结构参考（可选追加到提示词末尾）
```
参考目录结构：
demo/rustvulnweb/
├─ src/
│  ├─ main.rs               # 项目总入口，汇总所有测试访问链接
│  ├─ func_call.rs          # 通用跨文件函数调用、硬编码漏洞测试
│  ├─ common/
│  │  ├─ mod.rs             # common模块声明
│  │  ├─ vuln_parent.rs     # 公共父结构体，集中存放所有漏洞核心方法
│  │  └─ vuln_child.rs      # 独立子结构体，包含 VulnParent，封装调用方法
│  ├─ trait_def/
│  │  ├─ mod.rs             # trait模块声明
│  │  └─ vuln_trait.rs      # 独立trait定义文件
│  ├─ cmd_exec/             # 命令执行漏洞目录
│  │  ├─ mod.rs             # cmd_exec模块声明
│  │  ├─ sub_class_test.rs  # 纯结构体继承调用测试文件
│  │  └─ trait_impl.rs      # trait实现结构体调用测试文件
│  ├─ code_exec/            # 代码执行漏洞目录
│  │  ├─ mod.rs             # code_exec模块声明
│  │  ├─ sub_class_test.rs  # 纯结构体继承调用测试文件
│  │  └─ trait_impl.rs      # trait实现结构体调用测试文件
│  └─ ssrf/                 # SSRF漏洞目录
│     ├─ mod.rs             # ssrf模块声明
│     ├─ sub_class_test.rs  # 纯结构体继承调用测试文件
│     └─ trait_impl.rs      # trait实现结构体调用测试文件
├─ Cargo.toml               # 项目配置文件
└─ static/
   └─ index.html            # 测试入口页面
```